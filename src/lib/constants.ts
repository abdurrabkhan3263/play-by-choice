export const USER_LIMIT_SONG_LIST = 3;
export const SONG_LIMIT = 70;
export const messageForUserLimit = `You can't add more than ${USER_LIMIT_SONG_LIST} songs`;
export const messageForSongLimit = `You can't add more than ${SONG_LIMIT} songs`;
